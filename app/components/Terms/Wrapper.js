import styled from 'styled-components';
import SectionContent from 'canlaw-components/components/SectionContent';

const Wrapper = styled(SectionContent)`
  text-align: left;
  margin-top: 70px;
`;

export default Wrapper;
