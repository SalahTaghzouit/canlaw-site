import styled from 'styled-components';

const I = styled.i`
  font-size: 28px!important;
  color: ${(props) => props.theme.accent};
  display: block;
  width: 100%;
  clear: both;
`;

export default I;
