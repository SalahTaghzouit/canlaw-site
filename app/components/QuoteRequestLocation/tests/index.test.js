// import React from 'react';
// import { shallow } from 'enzyme';

// import QuoteRequestLocation from '../index';

describe('<QuoteRequestLocation />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
