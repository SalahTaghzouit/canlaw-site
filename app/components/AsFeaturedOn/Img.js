import BaseImg from 'canlaw-components/components/Img';
import styled from 'styled-components';
import { BRAG_IMG_HEIGHT } from './constants';

const Img = styled(BaseImg)`
  height: ${BRAG_IMG_HEIGHT}px;
`;

export default Img;
