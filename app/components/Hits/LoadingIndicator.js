import styled from 'styled-components';
import BaseLoadingIndicator from 'canlaw-components/components/LoadingIndicator';

const LoadingIndicator = styled(BaseLoadingIndicator)`
  margin: auto;
`;

export default LoadingIndicator;
